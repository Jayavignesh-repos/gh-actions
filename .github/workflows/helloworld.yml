name: helloworld-program
on: [workflow_dispatch]
jobs:
  first-job:
   runs-on: ubuntu-latest
   steps: 
    - name: hello-world
      run: echo "hello world"
    - name: bye
      run: echo "bye-world"
      
