name: first-code
on: [workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print-hello
        run: echo "hello world"
      - name: print-bye
        run: echo "bye-world"
